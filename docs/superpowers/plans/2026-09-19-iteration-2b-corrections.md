# Itération 2b — corrections de rendu constatées sur le Chrome de l'utilisateur

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Une seule tâche.

**Goal:** Corriger les défauts visibles dans le Chrome (GPU) de l'utilisateur mais pas en Chromium headless : texte délavé, rail haut manquant, médaillon 100 pts, infobulle, fond de colonne.

**Spec:** `docs/superpowers/specs/2026-09-19-iteration-2-fidelite-design.md` (§7) — amendements à ajouter en fin de tâche.

## Constats (captures 1:1)
- `refs/equip.png` : jeu, Personnage › Équipement (entrées « Paré pour l'aventure ! » ×2 à 10 pts, « Divin » 100 pts terminé le 11.12.2019 avec liste de 15 emplacements cochés, « Dragon des temps nouveaux » 100 pts). Fenêtre au même endroit (518,191).
- `/tmp/claude-1000/chrome_equip_win.png` : rendu du site dans le Chrome de l'utilisateur (fenêtre recadrée 886×592). `/tmp/claude-1000/cmp-chrome-vs-game.png` : montage site (gauche) / jeu (droite).
- Défauts : (1) noms et descriptions des succès rendus rose pâle au lieu de brun — les filtres SVG `url(#game-paper*)` etc. sont appliqués au conteneur **et à son texte**, et le Chrome GPU les applique différemment du headless ; (2) le bord supérieur de la fenêtre (rail vert au-dessus du bandeau turquoise, visible de part et d'autre de la plaque) manque ; (3) le score n'est pas centré dans l'écu et le médaillon 100 pts déborde (géométrie des paliers IV/V extrapolée) ; (4) titre de l'infobulle jugé trop sombre par rapport au jeu ; (5) une bande verticale transparente à gauche de la colonne de contenu dans certains états (vue « Professions », capture utilisateur `/tmp/claude-1000/-home-llyam-projects/98ba852d-0ea7-498b-a5b6-2d8c53fbd2a8/images/13.png`).

### Task 1 : corrections
**Files:** `tools/extract_assets.py`, `tools/assets_manifest.json`, `tools/cut_sprites.py`, `tools/sprites_manifest.json`, tests `tools/tests/*` ; `src/components/game/GameFilters.tsx` (à supprimer), `src/components/game/MedalBadge.tsx` (+css), `src/screens/MedalsScreen/MedalsWindow.tsx` (+css), `MedalEntry.module.css`, `MedalsNavigation.module.css`, `MedalsList.module.css`, `GameScrollbar.module.css`, `ProgressBar.module.css`, `GameTooltip.module.css`, tests.

- [ ] **Plus aucun filtre SVG côté navigateur.** Cuire les décalages de couleur dans les images : `tools/assets_manifest.json` gagne `"color_offsets": { "<chemin texture>": [dr, dg, db] }` appliqué après rognage dans `extract_assets.py` (valeurs actuelles de `GameFilters.tsx` : FrameNavigation, CategoryContent, FrameContent02, MedalPaper, MedalPaperComplete, ProgressBar, ProgressBarGauge) ; `tools/cut_sprites.py` gagne une option `"derive": { "from": "<sprite>", "matrix": [...20 valeurs...] }` (ou `offset`) produisant `pill-full-open` (depuis `pill-full` avec la matrice `game-pill-open`) et `scroll-up-off`/`scroll-down-off` (matrice `game-arrow-off`). Tests pytest pour l'offset et la matrice (pixels réels, clamp 0–255, alpha intact). Regénérer (`extract_assets.py --force --skip-video`, `cut_sprites.py`). Supprimer `GameFilters.tsx` et toutes les règles `filter: url(#…)` ; remplacer par les nouveaux sprites/textures. Le texte doit rester brun (`#6b3f0a`/mesuré) : vérifier au pixel sur la capture headless **et** demander une capture du Chrome utilisateur au contrôleur (il la fera).
- [ ] **Rail haut.** Mesurer sur `refs/equip.png` (ou `refs/astral.png`) la bande verte au-dessus du bandeau turquoise, de part et d'autre de la plaque (y 191→~226, x 518→574 et 1349→1404) ; découper `rail-top-left`/`rail-top-right` (ou un `rail-top` répétable + coins) et les poser dans `MedalsWindow` derrière la plaque. Comparer au zoom `/tmp/claude-1000/z_game_top.png`.
- [ ] **Médaillon 100 pts.** Mesurer sur `refs/equip.png` l'entrée « Divin » : position/taille du cadre d'icône, de l'écu, du texte « 100 », du chiffre romain ; corriger `TIERS[4]` (et extrapoler V à partir de IV plutôt que de I). Vérifier que le badge ne déborde pas du parchemin et que le score est centré dans l'écu pour les paliers I, II, IV (captures disponibles). Le jeu n'agrandit pas la texture : vérifier l'échelle réelle du palier IV.
- [ ] **Infobulle.** Mesurer la couleur du titre sur `refs/tooltip.png` (pixels pleins du glyphe, pas les bords) et l'ombre ; ajuster `GameTooltip.module.css` (probablement supprimer l'ombre sombre qui assombrit les traits fins, ou l'éclaircir).
- [ ] **Bande transparente.** Reproduire : `/succes`, déplier Personnage, cliquer « Professions » (1 succès), capture 1920×1009 ; si la bande apparaît, corriger `.contentBack` (couvrir toute la colonne 317→872 sans dépendre d'un `span` décalé). Sinon documenter.
- [ ] Gates : `npm test && npm run build && python3 -m pytest tools/tests -q`. Comparaison `tools/compare_refs.py` fenêtre entière vs `refs/equip.png` avec la vue Équipement (le mock n'a pas le même contenu : juger le chrome, le badge 100 et la couleur du texte). Amender la spec §7.1/§7.3 (rail haut, palier IV mesuré, « aucun filtre côté navigateur »). Commit(s) en français.
